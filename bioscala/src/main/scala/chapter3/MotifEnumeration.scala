package chapter3

import utils.DNAUtils._

object MotifEnumeration {

  
  
  def solve(dna : List[String], k : Int, d : Int) = {
    
    val generator = sequenceGenerator(k)
    
  }
  
}